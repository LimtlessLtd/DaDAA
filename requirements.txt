# Requires Python 3.10-3.12 (kokoro's phonemizer dependency, misaki, doesn't support 3.13/3.14
# yet - this is the version ceiling for the whole project, not just TTS).
# No separate espeak-ng system install needed - misaki's `[en]` extra pulls in espeakng-loader,
# which bundles a prebuilt espeak-ng for phonemizing invented fantasy names.
numpy>=1.24.0
aiohttp>=3.9.0
torch>=2.0.0
faster-whisper>=0.10.0
scipy>=1.10.0
pyttsx3>=2.90
gtts>=2.4.0
torchaudio>=2.0.0
chromadb>=0.5.0
watchdog>=4.0.0
sentence-transformers>=3.0.0
kokoro>=0.9.4
soundfile>=0.12.1

# Image generation (NPC portraits + event scenes, server/services/image_gen_service.py) - local
# Stable Diffusion via diffusers. GPU acceleration needs a CUDA-enabled torch build installed
# manually (e.g. `pip install torch --index-url https://download.pytorch.org/whl/cu121`) since
# the unpinned torch>=2.0.0 above resolves to the CPU-only wheel by default; without it, image
# generation still works but falls back to CPU (much slower, not a crash - see
# ImageGenService's startup log line for which device it actually landed on).
diffusers>=0.30.0
transformers>=4.40.0
accelerate>=0.30.0
Pillow>=10.0.0
